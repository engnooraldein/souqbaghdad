{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd artifacts/souq-baghdad && npm run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "workspaces": [
    "artifacts/*",
    "scripts"
  ],
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
